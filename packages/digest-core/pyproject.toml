[project]
name = "digest-core"
version = "0.0.0"
description = "Shared scaffolding for digest-style projects (pc-insurance-digest, macro-ai-digest). SCAFFOLD ONLY — see EXTRACTION_PLAN.md."
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/digest_core"]
